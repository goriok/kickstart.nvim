{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; case \"$f\" in *.md) \"$HOME/.local/share/nvim/mason/bin/markdownlint\" \"$f\" ;; esac; } 2>&1 || true"
          }
        ]
      }
    ]
  }
}
